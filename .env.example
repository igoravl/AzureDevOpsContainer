# Environment Configuration Template
# Copy this file to .env and customize the values

# SQL Server Configuration
# IMPORTANT: Change this to a secure password before deploying!
# Must be at least 8 characters with uppercase, lowercase, numbers, and special characters
SA_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD!123

# Azure DevOps Server Configuration
# Set this to the drive letter where the Azure DevOps Server ISO is mounted
DVD_DRIVE=D:

# Port Configuration (optional - defaults are defined in docker-compose.yml)
# AZUREDEVOPS_PORT=8080
# SQLSERVER_PORT=1433
